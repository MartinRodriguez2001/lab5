<div class="container mt-5">
  <h1 class="mb-4">Detalle del Chat</h1>

  <div class="card">
    <div class="card-body">
      <p><strong>ID:</strong> <%= @chats.id %></p>
      <p><strong>Sender ID:</strong> <%= @chats.sender_id %></p>
      <p><strong>Receiver ID:</strong> <%= @chats.receiver_id %></p>
    </div>
  </div>

  <%= link_to "Volver a la lista", chats_path, class: "btn btn-primary mt-3" %>
</div>
